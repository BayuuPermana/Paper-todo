# Fix Mobile Start Button & Footer Icons Implementation Plan

## Overview
Replace generic emojis in the Mobile UI with custom inline SVGs and styled buttons to match the "Paper-todo" aesthetic and improve UX.

## Current State Analysis
- `src/App.jsx`: Uses `🗄️` and `🎯` in `.bottom-matrix`.
- `src/PomodoroTimer.jsx`: Uses `▶️` and `⏸️` in header variant.

## Implementation Approach
- Use inline SVGs for icons to avoid external dependencies.
- Style SVGs with `stroke="black"` and `stroke-width="2"` to match the comic/hand-drawn style.
- Refactor `PomodoroTimer` header variant to use a proper button container.

## Phase 1: Bottom Matrix Icons (App.jsx)
### Overview
Replace Footer Emojis with "Paper" themed SVGs.

### Changes Required:
#### 1. `src/App.jsx`
**Changes**: Replace emoji spans with inline SVG code.

**Archive Icon (Box/Folder)**:
```jsx
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
  <path d="M21 8v13H3V8" />
  <path d="M1 3h22v5H1z" />
  <path d="M10 12h4" />
</svg>
```

**Focus Icon (Target)**:
```jsx
<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
  <circle cx="12" cy="12" r="10" />
  <circle cx="12" cy="12" r="6" />
  <circle cx="12" cy="12" r="2" />
</svg>
```

### Success Criteria:
#### Manual:
- [ ] Mobile view footer shows clean SVG icons instead of colored emojis.
- [ ] Icons respond to active state (color change).

## Phase 2: Pomodoro Header Button (PomodoroTimer.jsx)
### Overview
Replace Emoji buttons with a styled "Start/Pause" button.

### Changes Required:
#### 1. `src/PomodoroTimer.jsx`
**Changes**:
- Update the `header` variant return block.
- Replace `▶️` with a styled button containing a Play SVG.
- Replace `⏸️` with a styled button containing a Pause SVG.
- Add specific styling for the button (border, padding) to fix "whackness".

**Play SVG**:
```jsx
<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" stroke="none">
  <polygon points="5 3 19 12 5 21 5 3" />
</svg>
```

**Pause SVG**:
```jsx
<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" stroke="none">
  <rect x="6" y="4" width="4" height="16" />
  <rect x="14" y="4" width="4" height="16" />
</svg>
```

**Button Style**:
- `border: 2px solid black`
- `border-radius: 4px`
- `background-color: #f0f0f0` (default), `#e0e0e0` (active)
- `display: flex`, `align-items: center`

### Success Criteria:
#### Manual:
- [ ] Mobile header shows a clear, clickable "Start" or "Pause" button.
- [ ] Button alignment is centered with the timer text.

## Finalize
- [ ] Verify both changes on mobile view (<1200px).
